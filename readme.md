# VAE重构虚拟晶格
## 简介
- 随机生成一些格点，施加**高斯噪声**和**全局缩放因子**。
- 直接将格点坐标拉平并标准化作为输入。
- 使用简单的**变分自编码器**（VAE）重构晶格，损失函数包含**重构项**和**KL散度项**。

## 环境
- 基本的深度学习环境即可。

## 运行
- `model.py` 为VAE模型，`utils.py` 为各种自定义的工具函数。
- `config.yaml` 包含了部分训练超参数，运行 `train.py` 进行训练并保存最佳模型参数。
- `test.py` 加载模型参数并可生成 `.xyz` 轨迹文件用于可视化。